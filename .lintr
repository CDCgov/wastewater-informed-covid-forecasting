linters: linters_with_defaults(
    object_usage_linter = NULL,
    line_length_linter(length = 80),
    return_linter = NULL,
    indentation_linter = NULL
  )
exclusions: list(
    ".Rprofile"
  )
encoding: "UTF-8"
